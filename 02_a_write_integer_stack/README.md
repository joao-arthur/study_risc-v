# 02 - Write integer using stack

We need to convert digit by digit into an string in order to print it.
The stack pointer starts in a high address and grows down. In order to increment it we simply add how many bytes we want.