# 02 - Write integer using stack

We need to convert digit by digit into an string in order to print it.
The stack pointer `sp` starts in a high address and grows downwards. In order to increment it, we subtract the number of bytes we want.