# 02 - Write integer using ELF .BSS

We need to convert digit by digit into an string in order to print it.
`.BSS` is an `ELF` section where we can declare uninitialized variables that don't occupy any space on the binary file.
