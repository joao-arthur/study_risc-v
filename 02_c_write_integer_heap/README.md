# 02 - Write integer using heap

We need to convert digit by digit into an string in order to print it.
Linux has more than one syscall to allocate memory on the heap. In this example we use mmap. The heap starts on a low address and grows upwards.
